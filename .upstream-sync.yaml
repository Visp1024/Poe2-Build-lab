# Маркер синхронизации с upstream PathOfBuildingCommunity/PathOfBuilding-PoE2.
# Используется scripts/sync-upstream.ps1 для инкрементального 3-way патча.
# После успешного синка обновляется автоматически.

upstream_remote: upstream
upstream_url: https://github.com/PathOfBuildingCommunity/PathOfBuilding-PoE2.git
upstream_branch: dev

# Последний синхронизированный upstream commit и его версия.
# При первом синке = точка форка (Release 0.15.0).
last_synced_sha: a7dec3f5a8c2acb3790026dea73ebed96bcf024b  # Release 0.21.1 (incl. 0.5.3 + 0.5.4 data exports)
last_synced_version: 0.21.1
game_version: 0.5.4           # PoE2 game patch this sync targets — goes in the release archive name
last_synced_date: 2026-06-27

# Пути в upstream, которые мы тянем 3-way патчем.
# Avalonia/UI и runtime/*.dll НЕ трогаем.
# ВАЖНО: src/ целиком, а не список подкаталогов — при первом big-bang синке
# перечень подкаталогов пропустил корневые файлы src/ (GameVersions.lua не
# получил регистрацию дерева 0_5, HeadlessWrapper.lua — wipeGlobalCache в
# newBuild, LaunchServer.lua — IPv4-фикс). Форк-только файлы внутри src/ это
# не задевает: патч строится из diff upstream'а, наших файлов там нет.
sync_paths:
  - src/
  - runtime/lua/
  - manifest.xml
  - changelog.txt

# Пути, которые upstream менять не должен — наша территория.
ours_only:
  - PBLEngine/
  - PBLApp/
  - PBLApp.Core/
  - PBLMcp/
  - PBLExport/
  - PBLHost/
  - scripts/
  - CLAUDE.md
  - AVALONIA_MIGRATION_PLAN.md
  - LOCALIZATION_PLAN.md
  - UPDATE_PIPELINE.md
  - .upstream-sync.yaml
